spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    show-sql: true
    hibernate:
      ddl-auto: update

  flyway:
    baseline-on-migrate: true

logging:
  level:
    org.springframework.data: DEBUG
    org.springframework.security: TRACE