/* This environment is replaced with the environment.development in angular.json
* Configure for deployment
* */
export const environment = {
  production: true,
  apiUrl: "http://localhost:8080/api/v1", // Development url
  applicationName: "Auction App",
  pageSize: 8
};
